import { TabsHideDirective } from './tabs-hide.directive';

describe('TabsHideDirective', () => {
  it('should create an instance', () => {
    const directive = new TabsHideDirective();
    expect(directive).toBeTruthy();
  });
});
